"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Segment = void 0;
const enum_js_1 = require("../../utils/enum.js");
const exception_js_1 = require("../../utils/exception.js");
const utils_js_1 = require("../../utils/utils.js");
const field_js_1 = require("./field.js");
const nodeBase_js_1 = require("./nodeBase.js");
const subComponent_js_1 = require("./subComponent.js");
/**
 * Segment
 * @since 1.0.0
 */
class Segment extends nodeBase_js_1.NodeBase {
    /** @internal */
    constructor(parent, text) {
        super(parent, text, enum_js_1.Delimiters.Field);
        if (!(0, utils_js_1.isHL7String)(text) || text.length === 0) {
            throw new exception_js_1.HL7FatalError("Segment must have a name.");
        }
        this._segmentName = text.slice(0, 3);
        this._name = this._segmentName;
    }
    /** @internal */
    read(path) {
        let index = parseInt(path.shift());
        if (index < 1) {
            throw new exception_js_1.HL7FatalError("Index must be 1 or greater.");
        }
        if (this._name === "MSH" || this._name === "BHS" || this._name === "FHS") {
            if (typeof this.message !== "undefined" && index === 1) {
                return new subComponent_js_1.SubComponent(this, "1", this.message.delimiters[enum_js_1.Delimiters.Field]);
            }
            else {
                index = index - 1;
            }
        }
        const field = this.children[index];
        return typeof field !== "undefined" && path.length > 0
            ? field.read(path)
            : field;
    }
    /** @internal */
    set(path, value) {
        if (arguments.length === 1) {
            return this.ensure(path);
        }
        if (typeof path === "string") {
            if (Array.isArray(value)) {
                for (let i = 0; i < value.length; i++) {
                    this.set(`${path}.${i + 1}`, value[i]);
                }
            }
            else {
                const _path = this.preparePath(path);
                this.write(_path, this.prepareValue(value));
            }
            return this;
        }
        else if ((0, utils_js_1.isHL7Number)(path)) {
            if (Array.isArray(value)) {
                const child = this.ensure(path);
                for (let i = 0, l = value.length; i < l; i++) {
                    child.set(i, value[i]);
                }
                return this;
            }
            else {
                this.setChild(this.createChild(this.prepareValue(value), path), path);
            }
            return this;
        }
        throw new exception_js_1.HL7FatalError("Path must be a string or number.");
    }
    /** @internal */
    writeCore(path, value) {
        let index = parseInt(path.shift());
        if (index < 1 || isNaN(index)) {
            throw new exception_js_1.HL7FatalError("Can't have an index < 1 or not be a valid number.");
        }
        if (this._name === "MSH" || this._name === "BHS" || this._name === "FHS") {
            if (index === 1 || index === 2) {
                throw new exception_js_1.HL7FatalError("You cannot assign the field separator or encoding characters");
            }
            else {
                index = index - 1;
            }
        }
        return this.writeAtIndex(path, value, index);
    }
    /** @internal */
    pathCore() {
        return [this._segmentName];
    }
    /** @internal */
    createChild(text, index) {
        return new field_js_1.Field(this, index.toString(), text);
    }
}
exports.Segment = Segment;
